- есть пожелание: отображать ошибки в базе, типа файл не найден и другие
	примерное решение
		FarEditorSet.cpp:
  	parserFactory->getHRCParser()->setErrorHandler(mySuperCoolErrorHandler);
		выводить на экран, сообщение "есть ошибки, смотрите лог в таком то файле"

- Colorer 5 тормозит просто безбожно. У меня бывает относительно небольшие файлы (в основном C++/C#), 
	но как начинаешь редактировать - я печатаю быстрее, чем он пишет. Этож, блин, текстовый редактор, а 
	не Crysis. Centrino Duo T7500 (2x2.2). На Athlon 3500+ тоже самое. Про 70 мб я уж и не зарекаюсь. 
	В четвертом Colorer все радостно летает. Но в пятом схемы поприличнее (хотя мне не все нравится, но это уже не по теме).

--Ещё: открываю меню ошибок, и перемещаюсь с помощью Ctrl-Cursor
  Проблема в том что меню закрывает собой сами ошибки
  То же самое с меню Outliner list
  Меню, в отличии от диалога, не перетаскивается, поэтому надо позиционировать его интеллектуально сразу
  Так, чтобы не закрывать место ошибки (т.е. выше или ниже)

 -Ещё. При большом количестве элементов меню может быть слишком большим. Нужна настройка (максимальное количество элементов, после которого появляется скроллер)
  Помимо настройки можно ещё и хоткеи для этого.
  Можно позиционировать не меню, а сам текст. Но для текста может остаться слишком мало места...
  Если позиционировать меню, то наверно только при переходе. Т.е. при первом вызове меню должно быть как обычно - по центру, и, по возможности, без скроллера.

--Не могу понять одну псевдофичу, связанную с cross’ом.

  Если выставляю def:HorzCross в ровно те же самые цвета (fore и back), что и Editor Selected Text в настройках FAR’а, то Colorer красит всю текущую строку в эти самые цвета.
  Если хотя бы один из двух цветов (fore или back) отличается — раскраска идёт нормально (почти):
  элементы синтаксиса, для которых задан цвет текста и фона, остаются как есть;
  у элементов, для которых задан только цвет текста, меняется фон;
  у текста, не подпадающего под схему, меняется то и другое.

  Far x86 2.0.1354~MarkChar, colorer 1.0.2.3.